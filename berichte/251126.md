# Arbeitsbericht

- Thema: C in/out
- Name: Maximilian Mayr
- Klasse: 2AHITS
- Datum: 26.11.2025
- Fach: ITSI Übungen

## Übung 2.5(C in/out)

### Aufgabenstellung

Erstelle eine Datei sumieren.cpp

Schreibe ein C Programm das zuerst eine Anzahl `(int n)` einliest.

Dann werden n weitere ganze Zahlen eingelesen und die Summe dieser Zahlen ermittelt.

Compiliere das Programm mit folgender Kommandozeile:

`g++ -o summiere summiere.cpp`

Wenn keine Compile-Fehler auftreten wird im gleichen Verzeichnis das Programm mit dem Namen sum erzeugt. Prüfe mit ls ob vorhanden.

Behebe evtl. Compiler-Fehler und starte dann das Programm mit:

`./summiere`

Erstelle eine Textdatei mit den Input Daten für das Programm sum. Z.B.
Rufe nun das Programm auf und leite die Textdatei per Dateiumleitung ins stdin des Programms summiere um. Schreibe weiters den Output des Programms in eine Textdatei.

### Ausführung
---

```sh
~/workspace$ cd stdinout
~/workspace/stdinout$ touch sumieren.cpp
~/workspace/stdinout$ g++ -o summiere sumieren.cpp
~/workspace/stdinout$ ls
 etcdir.txt   made_by_echoing.txt   summiere    test.txt
 hallo.txt    sumieren.cpp          test2.txt  'Urlaubs Fotos'
~/workspace/stdinout$ ./summiere
3
5
2
4
11
~/workspace/stdinout$ cat >input01.txt
9
4
7
2
1
1
4
3
6
8
^C
~/workspace/stdinout$ ./summiere <input01.txt >output01.txt
~/workspace/stdinout$ cat output01.txt
36
```

### Inhalt von sumieren.cpp
---

```sh
#include <stdio.h>

int main(void)
{
  int n = 0;
  int sum = 0;

  scanf("%d", &n);

  int x[n];

  for (int i = 0; i < n; i++)
  {
    scanf("%d", &x[i]); 

    sum += x[i];
  }

  printf("%d\n", sum);

  return 0;
}

```
---
### Automatisierung mit shell-skript

mit `vim autosum.sh` in stdinout aufrufen
```sh
#!/bin/bash

g++ -0 summiere sumieren.cpp

./summiere <input01.txt >output01.txt

cat output.txt
```
mit `:wq` verlassen

```sh
~/workspace/stdinout$ touch autosum.sh
~/workspace/stdinout$ vim autosum.sh
vim: command not installed. Multiple versions of this command were found in Nix.
Select one to run (or press Ctrl-C to cancel):
vimHugeX  8.2.2567  The most popular clone of the VI editor
Adding vimHugeX to .replit
~/workspace/stdinout$ ./autosum.sh
bash: ./autosum.sh: Permission denied
~/workspace/stdinout$ chmod +x autosum.sh
~/workspace/stdinout$ ./autosum.sh
36
~/workspace/stdinout$ cat >input01.txt
6
2
4
5
8
1
4
^C
~/workspace/stdinout$ ./autosum.sh
24
~/workspace/stdinout$ vim autosum.sh
~/workspace/stdinout$ ./autosum.sh
24
~/workspace/stdinout$ 
```